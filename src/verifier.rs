use crate::analyze_code::Instruction;

pub fn verify_data(instructions: Vec<Instruction>) -> bool {
    let mut allowed: bool = true;

    for _i in 0..instructions.len() {
        
    }

    allowed
}